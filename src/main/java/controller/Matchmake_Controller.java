package controller;
import java.util.HashMap;
import model.Matchmake;
import model.Player;

public class Matchmake_Controller
{
    private HashMap<Player,Player> matchmake;

    public Matchmake_Controller()
    {
        
    }

    public void random_matchmake()
    {

    }

    public void matchmake(Player playerA, Player playerB)
    {
       //if this playerA has a matchmake with PlayerB, then don´t do matchmake
    }

    public void erase_matchmaking(Matchmake matchmake)
    {
        
    }
}